import type { AppConfig } from "@/lib/apps/types";

export const appConfig = {
  appName: "Simplify",
  description: "LaTeX 式の簡約のデモ",
  status: "published",
  tags: ["maths", "utility"],
  sortOrder: 11,
} satisfies AppConfig;
