import type { AppConfig } from "@/lib/apps/types";

export const appConfig = {
  appName: "Graph 3D",
  description: "3 次元グラフを描画するページ",
  status: "published",
  tags: ["maths"],
  sortOrder: 7,
} satisfies AppConfig;
