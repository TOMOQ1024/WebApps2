import type { AppConfig } from "@/shared/apps/types";

export const appConfig = {
  appName: "CompDynam",
  description: "複素関数の反復計算を可視化するページ",
  status: "published",
  tags: ["maths", "fractal"],
  sortOrder: 1,
} satisfies AppConfig;
