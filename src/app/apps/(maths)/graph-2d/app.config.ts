import type { AppConfig } from "@/lib/apps/types";

export const appConfig = {
  appName: "Graph 2D",
  description: "2 次元グラフを描画するページ",
  status: "published",
  tags: ["maths"],
  sortOrder: 2,
} satisfies AppConfig;
