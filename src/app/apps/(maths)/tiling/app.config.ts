import type { AppConfig } from "@/lib/apps/types";

export const appConfig = {
  appName: "Tiling",
  description: "平面充填を描画するページ",
  status: "published",
  tags: ["maths", "geometry"],
  sortOrder: 4,
} satisfies AppConfig;
