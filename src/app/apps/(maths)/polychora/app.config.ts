import type { AppConfig } from "@/lib/apps/types";

export const appConfig = {
  appName: "Polychora",
  description: "4 次元一様多胞体を観察するページ",
  status: "published",
  tags: ["maths", "geometry"],
  sortOrder: 3,
} satisfies AppConfig;
