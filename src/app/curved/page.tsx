import './app.scss';
import MainWrapper from './components/MainWrapper';

export const metadata = {
  title: 'Curved',
  description: '非ユークリッド幾何の描画'
}

export default function Main(){
  return <MainWrapper/>;
}
